# === Data / outputs (do NOT commit) ===
dataset/DIOR/
dataset/RSAR/
dataset/RSAR_bak_*/
dataset/_downloads/
# Keep work_dirs outputs untracked by default, but commit lightweight summaries.
work_dirs/*
!work_dirs/results/
work_dirs/results/*
!work_dirs/results/metrics.csv
!work_dirs/results/ablation_table.md
.rd_queue/

# === Weights (downloaded artifacts) ===
baseline/
weights/

# === Vendored/third-party checkouts (cloned on demand) ===
third_party/

# === Python cache ===
__pycache__/
*.py[cod]

# === Logs / temporary files ===
*.log
*.log.json
*.tmp
.pytest_cache/

# Environment snapshots are machine-specific; keep local by default.
docs/env_lock.txt
docs/system_info.md
